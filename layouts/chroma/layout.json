{
  "name": "chroma",
  "version": "1.0.0",
  "description": "CHROMA statement layout: a live palette control bar (presets + generative randomiser) that re-skins the page via CSS custom properties, persisted to localStorage. Pairs with the chroma theme.",
  "author": "OpenDigitalCC",
  "themes": ["chroma", "chroma-dusk"],
  "default_theme": "chroma",
  "tokens": {
    "colours": ["accent", "accent-2", "bg", "border", "heading", "on-accent", "surface", "surface-2", "text", "text-muted"],
    "fonts": ["body", "display"]
  }
}
